server:
  port: 9004

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.194.137:1111 #Nacos服务注册中心地址

management:
  endpoints:
    web:
      exposure:
        include: "*"
